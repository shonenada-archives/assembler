DATAS SEGMENT
DATAS ENDS

STACKS SEGMENT
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX, 3000H
	MOV DI, AX
	MOV AX, 0000H
	MOV CX, 0FH
AGAIN:
    MOV [DI], AL
	INC DI
	INC AL
	LOOP AGAIN
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
