DATAS SEGMENT
DATAS ENDS

STACKS SEGMENT
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AH,1
    INT 21H
    CMP AL,1BH
    JZ HALT
    MOV BL,AL
    MOV CH,2
    MOV DL,3AH
    MOV AH,2
    INT 21H
TRANSFORM:
    MOV CL,4
    ROL BL,CL
    MOV AL,BL
    AND AL,0FH
    ADD AL,30H
    CMP AL,3AH
    JL PRINT
    ADD AL,7H
PRINT:
    MOV DL,AL
    MOV AH,2
    INT 21H
    DEC CH
    JNZ TRANSFORM
    MOV DL,0DH
    INT 21H
    MOV DL,0A H
    INT 21H
    JMP START
HALT:
    MOV AX,4C00H
    INT 21H
CODES ENDS
    END START
