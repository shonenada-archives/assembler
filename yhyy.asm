
SSTACK	SEGMENT STACK
		DW 32 DUP(?)
SSTACK	ENDS

CODE	SEGMENT
		ASSUME CS:CODE
START:	MOV DX, 0646H
		MOV AL, 81H
		OUT DX, AL
		MOV BX, 0000H
AA1:
    	MOV DX, 0640H
		MOV AL, BL
		OUT DX, AL

		MOV DX, 0642H
		MOV AL, BH
		OUT DX, AL

		ROL BX
		
		CALL DELAY
		CALL DELAY
		JMP AA1
		
DELAY:	PUSH CX
		MOV CX, 0F000H
AA2:	PUSH AX
		POP  AX
		LOOP AA2
		POP  CX
		RET
		
CODE	ENDS
		END  START